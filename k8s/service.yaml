apiVersion: v1
kind: Service
metadata:
  name: healer-app
  namespace: healer
spec:
  selector:
    app: healer-app
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
  type: NodePort
